spring:
  datasource:
    url: jdbc:postgresql://ep-icy-math-a10ww8ba-pooler.ap-southeast-1.aws.neon.tech/neondb?user=neondb_owner&password=npg_VHDydwbhGC73&sslmode=require&channelBinding=require
    username: neondb_owner
    password: npg_VHDydwbhGC73
    driver-class-name: "org.postgresql.Driver"

  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    database-platform: org.hibernate.dialect.PostgreSQLDialect

brevo:
  api:
    key: ${BREVO_API}

server:
  port: 8080

jwt:
  secret: ${JWT_SECRET}
  expiration: 86400000

razorpay:
  key: ${RAZORPAY_KEY}
  secret: ${RAZORPAY_SECRET}

logging:
  level:
    root: INFO
    com.payae: DEBUG